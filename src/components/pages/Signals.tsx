interface SignalsProps {
  language: 'en' | 'fa';
}

const Signals = ({}: SignalsProps) => {
  return (
    <div className="p-6">
      {/* Empty page - ready for development */}
    </div>
  );
};

export default Signals;
