interface NewsProps {
  language: 'en' | 'fa';
}

const News = ({}: NewsProps) => {
  return (
    <div className="p-6">
      {/* Empty page - ready for development */}
    </div>
  );
};

export default News;
